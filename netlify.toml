[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Edge Functions configuration
[[edge_functions]]
  path = "/api/chat"
  function = "chat"

# SPA redirect rule - fixes 404 errors for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200